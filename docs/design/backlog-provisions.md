# Backlog provisions — TBD, not scheduled

*2026-09-03. Items raised by prospects or design review that are explicitly NOT current work. Each entry records the trigger that would promote it, the intended shape, and the code it would touch, so it can be picked up without re-deriving. Nothing here is committed to.*

| # | Provision | Trigger | Shape | Touches |
|---|---|---|---|---|
| P1 | **Signals-only tier** (accounts + signals, no KPIs) | first prospect with CRM notes/tickets but no usage data — expected to be the common case | journey series keyed to calendar months from the first signal (`kpi_only` null); arc rules with health-free variants (`had_negative_phase` and `below_healthy` become optional); leading composite and tripwires unchanged; header shows "no KPI layer" honestly | `journeys/journey_builder.py` (`leading_series`, `detect_phases`), `journeys/arc_classifier.py` (RULES `health_*` predicates), `journeys/features.py`, canvas Band 2 tile |
| P2 | **Once-sold / installed-base motion** | first hardware, capital-equipment or perpetual-license prospect | outcome vocabulary with decision dates (`maintenance_renewed`, `refresh_won`/`refresh_lost`, `repeat_order`, `attach`, `support_lapsed`, `decommissioned`) in the taxonomy buckets; catalog = `dc2_s` reused or a `installed_base` catalog; installed-base metric names in Wizard B (retention / attach / repeat-purchase / refresh win); lifecycle stages deploy → adopt → utilize → refresh; a demo manifest shaped for a refresh cycle | `config/taxonomy_base.json` (+ overlay), `wizards/wizard_b_hindsight.py` (`realized_nrr` labels), `utils/lifecycle_stages.py`, `demo/manifests/` |
| P3 | **Legacy-source adapters** (Gainsight, ChurnZero) | first prospect offering an export | per-source adapter: export → the four canonical CSVs, with declared transforms in config (CTA/play → decision with owner; timeline/notes/alerts → signals; scorecard/usage → KPIs; renewal/churn/downsell → outcomes with dates); ChurnScore polarity inverted, Gainsight letter grades non-numeric — explicit, never silent | new `adapters/` package + `config/source_*.json`; `utils/csv_upload.py` unchanged |
| P4 | **Incumbent-score comparator** in the backtest | with P3, or any tenant that supplies a prior tool's health history | their score turning red = a fourth "someone noticed" timestamp alongside leading / trailing / CSM flag; ledger row and canvas label; never imported as our health | `evals/lead_time_backtest.py` (`LAYERS`, `_warning_months`), forecast ledger, `config/taxonomy_base.json` (`incumbent_flag` role, leading-excluded) |
| P5 | **Bulk history ingest** (an export is the past, not today's upload) | with P3 | months in the export are closed months: immutability applies, `as_of` = last exported month, scrubber and backtest treat it as history; `data_origin` = the source system | `mcp_server/process_data_pipeline.py` (`calculate_health_scores` reopen rule), `journeys/wizard_a.py` |
| P6 | **Accountability model** — proposal A | team decision on who is accountable (CSM vs account team) | `actor` / `owner` on episodes, typed INVOLVES edges (about / owns / accountable_for), silent-sponsor and no-owner flags, scorecard from evidence | `journeys/journey_builder.py`, `models.py`, CSV contract (`owner` on decisions, stakeholder `since`/`until`), canvas people lane |
| P7 | **Investment lens** — proposal B | team decisions on product attribution source, unit of effort, $ vs priority | pillar drag from `contributing_pillars`; product tags on KPIs + product column on signals; lever by pillar role; portfolio investment map; effort-vs-need quadrant once effort is logged; exposure-weighted priority before dollars | `journeys/features.py`, catalogs (`product` on KPIs), `csv_schemas.json`, portfolio surface (new) |
| P8 | **Forecast block as a platform output** (today computed in the mock's JS) | before any surface other than the mock renders Band 3 | `forecast` on journey v3: leading hold/silence date, next-phase prior from the tenant transition matrix, likelihood range with `source` + `confidence_semantics`, tripwires, recommended actions with observed lift; citation-enforced narrative block | `journeys/` (new `forecast.py`, `narrative.py`), `journeys/wizard_a.py` |
| P9 | **Export pipeline** (PNG/PDF/SVG from the canvas) | first deck that needs a canvas image | Playwright in the app container; bundled Plex fonts; wait on `data-rendered`; pinned 1400 @2x; `--now` for time-travel frames | `exports/` (new), `Dockerfile` |
| P10 | **Ask AI over the journey/forecast contract** | before Ask AI is re-introduced in the new build | a question interface over journey v3 + forecast block, not over raw tables: every claim cites an episode / ledger row / pattern row; numbers are read from the blocks, never generated; "why" states its evidence tier; absence of evidence is an answer; time-travel questions use the scrubber semantics; portfolio questions aggregate the same objects | `journeys/`, forecast + narrative blocks (P8), MCP journey tools; the old repo's Ask AI tool registry is NOT ported as-is |
| P11 | **Role-specific readings as RBAC defaults** | with RBAC | the CFO / CRO / VP-CS / CSM readings of the same canvas (exposure + contraction scenario · competitive deal + exec owner · process rules + portfolio scan · Friday tasks + customer-side "yes") become the default framing per role for both the canvas header and Ask AI; same evidence, different emphasis | canvas header, Ask AI prompt framing, portfolio surface |
| P12 | **Evidence-layer overlay for Gainsight / ChurnZero accounts** (upsell packaging) | first prospect on an incumbent scoring engine | coexist read-only: adapters (P3) + incumbent comparator (P4) + bulk history (P5) packaged as "keep your score and workflows; we add why, who, how early, how sure — proved on your own history"; differentiate on evidence discipline (tiers, accountability, backtest, honesty labels), not on reading emails — both vendors ship signal/AI features (Gainsight/Staircase AI) | P3–P5, sales collateral |
| P13 | **Once-sold / no-telemetry onboarding** (greenfield) | first once-sold prospect without an incumbent | P1 (signals-only tier) + P2 (installed-base outcome vocabulary) + connectors to where behavior lives (field service, partner portals, support) + outcome-logging as an onboarding step, since refresh won/lost is rarely recorded | P1, P2, connectors |
| P14 | **Pylon connector** (behavioral source) | first prospect on Pylon, or the signals-only tier (P1) going live | tickets + shared Slack/Teams channels → signals with system timestamps and real people (fixes the CRM-hygiene problem at the source): escalation velocity, sentiment shifts, who stopped/started replying; resolution events as episodes; Pylon's own account health = incumbent comparator (P4), never our score | `signal_engine/` connectors, taxonomy roles, P4 |
| P15 | **Clay connector** (external evidence + people resolution) | first prospect using Clay, or when unresolved-people counts matter | enrichment → external-context signals already in the taxonomy (`funding_raised`, `champion_change` via job change, `commercial_pressure`); resolve people named in evidence but missing from the roster (Alex Patel case); never a trigger→outbound workflow on our side — Clay fires triggers, we judge them against the account's evidence | `signal_engine/`, people/accountability model (P6) |
| P16 | **Signal engine v2 — the ingestion/normalization layer for signals-first** (assessed 2026-09-03) | before P1 (signals-only tier) or any connector (P14/P15) | keep `enrich_signal`, budget tracking, collision windows, urgency. Reshape the pipeline: (1) mount ingest on the HTTP server (`/api/signals/*` beside `/mcp`, + MCP `submit_signal`) — today the blueprints are unmounted, nothing can reach the engine; (2) `intent → taxonomy role` table in config; the worker writes ROLES, never `node_subtype='qualitative_signal'` (third-vocabulary bug, same class that killed Wizard B); (3) structured sources (tickets, CRM activity, Pylon) rule-map category/priority → role without LLM; LLM only for free text; (4) polarity reconciliation (role wins, conflict flagged) + content-hash dedup; (5) people resolution → roster / about-owns edges, unresolved kept + flagged; (6) retire `fusion` pillar modifiers — the journey composite is the ONLY leading score (absolute-separation rule); (7) outcome-logging path (MCP tool; billing connector later); (8) per-account coverage stats (sources, last touch, months without evidence); (9) `as_of` on bulk ingest so history is history; (10) provenance on written nodes (source_platform, tier) | `signal_engine/{ingest_api,worker,collision,fusion,enrichment}.py`, `server.py`, `config/taxonomy_base.json`, `journeys/` |

Positioning vs adjacent tools: Clay = pre-sale (who to sell to), Pylon = in-life service (serve them), we = in-life revenue outcome (what happened, who, how early, how sure) — both are sources, not rivals.

Related: `journey-canvas-proposal.md` (P6, P7 in full), `journey-canvas.md` §7 (P8, P9), `demo-narratives.md`.
