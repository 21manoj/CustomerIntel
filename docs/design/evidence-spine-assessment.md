# Evidence spine — assessment against the "vertical AI" principles, and what it opens up

*2026-09-04. Code-verified against `backend/` at commit `1117aa5`. Companion to `wizard-a-assessment.md` (§6 states the deciding criterion: "a credible AI platform grounded in truth"), `journey-canvas.md`, `journey-canvas-proposal.md`, `backlog-provisions.md` (P1–P16), `demo-narratives.md`. No code was changed for this document.*

---

## 0. Recovering the original discussion

Searched all ten session transcripts under `~/.claude/projects/-Users-manojgupta-CustomerSuccessAI-DataCenter/*.jsonl`, the memory directory, and `CustomerIntel/docs` for "Google", "legal", "finance", "vertical AI", "Gemini Enterprise", "evidence needs a surface", "grounded". **Nothing was found except the question itself** (this session's transcript, 2026-09-04). The earlier conversation either happened outside these transcripts or was not retained; no memory note records it. The only nearby phrase is the user's criterion in `wizard-a-assessment.md` §6 ("grounded in truth").

So Q1 is answered against the two principles the user named plus the four such announcements consistently stress: **(1) evidence needs a surface, (2) every output grounded in data, (3) human review in the loop, (4) auditability / provenance, (5) domain-specific evaluation, (6) confidence that means something.** If the original list differs, the table below maps onto it easily.

## 1. Question 1 — are the principles taken care of?

Short answer: **the data layer is built to these principles; the surfaces, the human loop, and the extraction eval are not.** Three Done, two Partial, one Missing.

| # | Principle | Verdict | Where it is honoured | The gap |
|---|---|---|---|---|
| 1 | Evidence needs a surface | **Partial** | Every OBSERVED node carries the verbatim `quote` as its title and in `properties` (`signal_engine/pipeline.py:252,266`); episodes cite `evidence_node_ids` (`journeys/journey_builder.py:41,103`); the arc cites `supporting_episode_ids`, `alternatives` with what they were missing, `contradicting_evidence` (`journeys/arc_classifier.py:185-196`); phase boundaries cite `trigger_episode_id` (`:255`); each month of the leading series lists `contributing_episode_ids` (`:316`). | **There is no surface.** The only renderer is the static mock, built from an exported JSON file (`docs/design/mocks/build/build_canvas.py` reads `zenith_journey.json`; no `fetch(` in any mock). The server exposes seven MCP tools (`create_customer`, `upload_csv`, `process_data`, `trigger_wizard`, `submit_signal`, `process_signals`, `configure_signal_engine`) and the `/api/signals/*` ingest/process/review-queue/status routes (`signal_engine/http.py:406-412`) — **no route or tool returns a journey, an episode, or an evidence node**. The citation-enforced story paragraph is design only (`journey-canvas.md` §2; P8). |
| 2 | Outcomes grounded in data | **Done** (data layer) | No fallback arc: `steady` or `unclassified` with a stated reason (`arc_classifier.py:6-12, 198-208`). Wizard A writes no ContextNode/ContextEdge rows (`journeys/wizard_a.py:13-14`; `test_no_synthetic_nodes_or_edges_written`). The extraction vocabulary is a tool-schema enum, so an invented subtype is rejected by the API (`signal_engine/enrichment.py:117-118,135`); anything that slips through is dropped and counted (`:247-248`). A failed extraction is not evidence — the signal stays queued (`pipeline.py:346-352`). The keyword stub is always `requires_review` (`enrichment.py:373`). The backtest labels a result `measured` only when `data_origin` is NULL **and** the caller asserts `--real` (`evals/lead_time_backtest.py` docstring). | The forward-looking block (outlook, likelihood fan, tripwires) is computed in the mock's JavaScript (`journey-canvas-proposal.md` §2.2) — the one part of the story a buyer cares most about is not yet a platform output (P8). |
| 3 | Human in the loop | **Missing** | `GET /api/signals/review-queue` lists `requires_review` rows (`http.py:125-133`, `ingest_api.py:121-139`). `approval_queue.py` has approve/reject routes (`:393,410`) and an `ApprovalRequest` model. | The review queue is **read-only**: no accept / reject / reclassify write path, and **nothing downstream reads `requires_review`** — outside `signal_engine/` it appears only as a column (`models.py:411`). A low-confidence node enters the leading composite (`journey_builder.py:276-279`) and the arc rules with the same weight as a high-confidence one. `approval_queue`'s blueprint is never mounted on the FastMCP server (`server.py` registers only signal routes; `auth_middleware.py` is a `NotImplementedError` placeholder), so it is importable, not reachable. No table records a human override. |
| 4 | Auditability / provenance | **Done** for machine provenance, **Partial** for the rest | Node: `source='observed'`, `source_platform=<source_type>`, `source_event_id=<signal_id>` (`pipeline.py:264-267`); properties carry `classification_basis` (`declared_subtype` / `llm_extraction` / `llm_intent` / `unclassified`), `llm_model_version` (`structured_rule_map` for the rule path, `stub_keyword_v2` for the stub), `confidence`, `requires_review`, `evidence_tier`, `polarity_conflict`, `raw_sentiment_score`, `person_unresolved` (`:247-263`). The signal row keeps `raw_text`, `extractions`, `content_hash`, `source_ref`, `occurred_at` (`signal_engine/models.py:11-36`). Evidence tiers observed / inferred / synthetic in `utils/provenance.py:35-37`. Every LLM call is metered (`enrichment.py:326-329`). `WizardRun` is an audit row per wizard (`models.py:552`). | No **prompt version** or **taxonomy version** on the node (`taxonomy_base.json` is `"version": "0.1"`; prompt v2 landed in `b474683` but only the model id is stamped) — after the next prompt change, old and new extractions are indistinguishable. Journeys are **upserted in place** (`wizard_a.py:57-69`): there is no record of what the journey said last month, which the scrubber and any "what did we know when" audit require. `ContextNode.confidence` is never set by the pipeline, so the column holds its default `1.0` while the real value sits in `properties.confidence` (`pipeline.py:264-268`; `models.py:246`). |
| 5 | Domain-specific evaluation | **Missing** | 308 tests; `evals/lead_time_backtest.py` with pre-registered thresholds (`PREREGISTERED = {median ≥ 60 d, recall ≥ 0.70, FA ≤ 5/100}`), right-censoring, refutation, evidence label. Taxonomy carries role definitions ("is / not") and few-shot examples per vertical (`taxonomy_base.json`, 17 roles; overlays 105–112 subtypes). | **No extraction eval.** The signal-engine tests mock the LLM (`tests/test_signal_engine_v2.py:187-232`, `fake_llm`). There is no labelled set of communications with gold subtype / role / person, hence no precision, recall, or per-vertical confusion matrix, and no measurement of polarity-conflict rate, unresolved-person rate, or unclassified rate per source (P16 item 8, "coverage stats", is still open). The one eval we have measures *lead time*, which presupposes the extraction is right. |
| 6 | Confidence that means something | **Partial** | Semantics are declared where they are not calibrated: arc `confidence_semantics: 'rule_match_constant'` (`arc_classifier.py:189`), edge `'role_match_heuristic_constant'` (`mcp_server/process_data_pipeline.py:417`), Wizard B interventions "a comparison, not a causal estimate", the forecast fan "prior, not calibrated" in design. Extraction confidence has a stated meaning — explicitness: 1.0 stated / 0.7 strongly implied / 0.5 weakly implied (`enrichment.py:56`), threshold from config. | Extraction confidence is **LLM self-report** and carries no semantics label on the node (should read e.g. `llm_self_report_explicitness`). Nothing is calibrated against outcomes anywhere — by design until a tenant has events, but it should be stated on every number the way the arc does. The `1.0` default on `ContextNode.confidence` (row 4) actively misleads anything that reads the column. |

**Net.** The spine is honest: quotes, citations, no fabrication, no fallback, provenance on every node, thresholds pre-registered. What is missing is everything a *person* touches — a read surface, a review write path, an audit of overrides, and a labelled eval of the extractor. Those four are also the four cheapest items in §2.

## 2. Question 2 — what "the spine is the evidence, not the score" opens up

Ranked by nearness. Sizes: S = days, M = 1–3 weeks, L = a quarter or dependent on real data.

| # | Opportunity | Why the evidence spine enables it (a score can't) | Exists in repo | Missing | Size |
|---|---|---|---|---|---|
| 1 | **Evidence read surface + Ask AI over the contract** (P10) | An answer that cites an episode id and a quote is checkable; an answer that cites "health 62" is not. Canvas and answer share one JSON so they cannot disagree. | Journey v3 JSON (`JourneyData.journey_json`), `WizardRun.results`, episodes with quotes and provenance. | `get_journey` / `get_evidence` / `get_review_queue` read tools; a citation-enforced narrative generator (P8); a rule that numbers are read, never generated. | S (read tools) + M (Ask AI) |
| 2 | **Evidence-backed QBR / renewal pack** | The pack is the journey: phases with their triggers, the quotes behind each warning, the intervention's before/after, realized vs narrative dollars as two numbers. A score-centric tool exports a chart and a paragraph nobody can verify. | Phases, `counterfactual_hooks`, `lead_days`, realized NRR (`wizards/wizard_b_hindsight.py:137`), `expected_path`. | A renderer (P9 export) and the narrative block; `owner` on decisions so the pack names who did what (P6). | S–M |
| 3 | **Auditable early-warning claim for CFO / board** | "Median D days before the decision in K of N, F false alarms, on your history" is a statement a finance function can accept because each of the N events opens to its evidence. | Backtest with pre-registered thresholds, right-censoring, three comparators, `evidence_label`, per-tenant reporting; Wizard B embeds it at onboarding. | A real tenant (`data_origin` NULL); load-driver `data_origin` stamping; per-account coverage stats; a ledger surface. | S (code) / L (data) |
| 4 | **Counterfactual "what would have warned"** (Wizard B Hindsight) | Rules are *derived* from which roles preceded which events on this tenant, with observed frequency and median lead (`wizard_b_hindsight.py:224`) — legible, refutable, and per-tenant. A learned score offers no such sentence. | `derived_rules`, transition matrix with triggers, `interventions` before/after, the scrubber semantics (design). | Point-in-time journey snapshots (journeys are upserted, §1 row 4); the incumbent's own score as a fourth comparator (P4). | S–M |
| 5 | **Signal-quality SLAs and "graph quality as a product surface"** | Only an evidence pipeline can report on itself: unclassified rate, polarity conflicts, unresolved people, duplicates, review backlog, months without evidence — per source and per connector. This becomes the buyer's data-quality report and the connector SLA. | `requires_review`, `polarity_conflict`, `person_unresolved`, `unclassified_signal`, `dropped_unknown_subtypes`, content-hash dedup, `process_pending` counters. | Coverage stats (P16-8), a labelled extraction eval (§1 row 5), the review write path, a stats endpoint. | M |
| 6 | **Incumbent overlay on Gainsight / ChurnZero** (P3, P4, P5, P12) | "Keep your score; we add why, who, how early, how sure — proved on your own history." The wedge is the backtest against *their* red flag; the differentiator is evidence discipline, since both vendors already read emails. | Backtest layers, taxonomy overlays, bulk-history semantics in design, `Customer.data_origin`. | Source adapters, `incumbent_flag` role, closed-month ingest, sales packaging. | M |
| 7 | **Signals-only, once-sold and no-telemetry tiers** (P1, P2, P13) | Where there is no usage KPI — hardware, capital equipment, perpetual licences — behaviour is all there is; scoring engines degrade to manual scorecards. | The whole pipeline is health-free except the series and some arc predicates. | `build_journey` keys `points` to `HealthScore` rows (`journey_builder.py:373-381`) and `detect_phases` / `leading_series` iterate them; arc rules carry `health_*` predicates. Needs calendar-month series, health-free rule variants, installed-base outcome vocabulary. | M |
| 8 | **Accountability lane + investment lens** (P6, P7) | People are already nodes named *in evidence* (`stakeholder_name/role`, `person_unresolved`); "silent sponsor" and "no owner" become named findings rather than empty bars. Pillar drag and product attribution point budget at a cause. | Roster resolution (`pipeline.py:126-172`), stakeholder meta on episodes, `contributing_pillars` per month, `pillar_roles` in catalogs. | `owner` on decisions, typed `about / owns / accountable_for` edges, product tags, effort logging (largest). | M |
| 9 | **Agentic action layer on cited evidence** | An action that quotes the sentence that triggered it, carries the role's urgency floor, and is measured by the next 90 days of evidence is governable; an action triggered by "score fell 8 points" is not. | `effective_urgency` per node, `suggested_action` per signal, `ApprovalRequest` with tiered thresholds, `counterfactual_hooks` for measurement. | Mount the approval routes behind real auth; an actuator (Slack / email / task); tie approvals to episode ids; measure lift on evidence. | M–L |
| 10 | **M&A due diligence evidence trail** | A PE buyer wants "show me every account whose behaviour says the revenue is leaving, with the quotes" — an evidence trail per account with provenance and realized NRR, not a health colour. | Per-account episodes with provenance, realized NRR, coverage, `evidence_label`. | DD vertical catalog, read-only scoped access, bulk history (P5), the read surface (#1). | M |
| 11 | **GRC / AI-governance reuse** | The same machinery — provenance tiers, `llm_model_version`, enum-enforced vocabulary, budget metering, approval tiers, pre-registered evals — is what an AI-governance product needs to audit *other* agents. | `utils/provenance.py`, `llm_budget_controller.py`, `approval_queue.py`, catalog auto-discovery. | Goal / reasoning / action node vocabulary, governance KPI catalog, regulatory export formats. | L (separate product) |
| 12 | **Community domain intelligence over evidence** | "Signal DNA" — which roles precede which events, with lead — pools across tenants in a way that calibrated *weights* never could, because the unit is an observable role, not a tenant-specific score. | `derived_rules` and transition matrices per tenant. | ≥ 5 real tenants per vertical, anonymisation, pooling, opt-in. | L |

The first five share one prerequisite: **a read surface over journey v3 and its evidence nodes**. Build that once and #1–#5 become days, not weeks.

## 3. What NOT to build yet

- **Ask AI before the read tools and the narrative block exist** — it would answer from raw tables, which is exactly what P10 forbids.
- **The forecast block as a product output** (P8) before a tenant has real events — the mock's fan is already labelled "prior"; shipping it live invites reading it as a prediction.
- **Dollars per pillar / per-playbook ROI** — no tenant has a calibrated health → retention relationship; exposure-weighted priority is the honest ceiling (`journey-canvas-proposal.md` §4.4).
- **The agentic actuator** before the review write path, real auth, and an override audit — otherwise the first autonomous action has no human record behind it.
- **Community domain intelligence and the GRC fork** — both need real cross-tenant data or a separate product decision.
- **A second extraction model or prompt tuning** before a labelled eval exists — there is nothing to measure improvement against.
