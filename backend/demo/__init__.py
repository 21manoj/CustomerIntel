"""
Demo tenants — constructed to the lead-time protocol, never presented as
measured. See docs/design/demo-narratives.md.

    python -m demo.generate --manifest demo/manifests/demo_silent_displacement_dc.json --out-dir /tmp/x
    python -m demo.generate --manifest ... --register     # creates the tenant via the MCP tools
"""
